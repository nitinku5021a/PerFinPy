#
# Python version guard:
# Fail fast if someone tries to install with a non-3.11 interpreter.
# (This "package" doesn't exist, but it's only required when python_version != 3.11.)
perfinpy-python311-only==0; python_version != "3.11"

Flask==2.2.5
Flask-SQLAlchemy==2.5.1
SQLAlchemy==1.4.48
python-dateutil==2.8.2
Werkzeug==2.2.3
gunicorn==20.1.0
setuptools<81
waitress==2.1.2
Flask-Migrate==4.0.4
openpyxl==3.1.5
